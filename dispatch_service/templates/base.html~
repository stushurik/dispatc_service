{% load static %}

<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN"
    "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html>
<head>
    <title>
        {% block title %}No title{% endblock %}
    </title>
<style>
  	body { background: url(image/fon1.jpeg) no-repeat ;
	background-size: 100%;}
	</style>

        {% block extra %}
        {% endblock %}
</head>

<body>
    {% if user.is_anonymous %}
        <a href="{% url 'login' %}">Login</a> |
        <a href="{% url 'registration_register' %}">Register</a>
    {% else %}
        <h7>Hello, <a href="/"><strong>{{ user }}</strong></a> |
            <a href="{% url 'logout' %}">Logout</a>
        </h7>
    {% endif %}

    <strong> || </strong>

    {% if events_count %}
        You have <a href="/my-events">{{ events_count }}</a> events

        <strong> || </strong>
    {% endif %}

    {% if messages %}
            {% for message in messages %}
            <span {% if message.tags %} class="{{ message.tags }}"{% endif %}>
                {{ message }}
            </span> |
            {% endfor %}
    {% endif %}

    <hr>
    {% block body %}
        {% block content %}
        {% endblock content %}
    {% endblock body %}
</body>
</html>
