{% extends "base.html" %}
{% block content %}
    {% if event.id %}
    <h1>Edit Event</h1>
    {% else %}
    <h1>Add Event</h1>
    {% endif %}
    
    <form action="{{ action }}" method="POST">
        {% csrf_token %}
        {{ form.as_p }}
        <input type="submit">
    </form>

    <a href="/">cancel</a>

{% endblock content %}